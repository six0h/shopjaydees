{
  "name": "shopjaydees-leadgen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc",
    "gcp-build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "npx functions-framework --target=discover --source=dist/index.js",
    "typecheck": "tsc --noEmit",
    "report": "npx tsx scripts/monthly-report.ts"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
